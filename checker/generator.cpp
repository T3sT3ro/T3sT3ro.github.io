#include <cstdio>
#include <random>
#include <ctime>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
	int seed, A, B;
	scanf("%d %d %d", &seed, &A, &B);
	srand(seed);
	printf("0 0 0");
	return 0;
}