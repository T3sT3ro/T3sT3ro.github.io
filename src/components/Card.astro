---
import '@styles/theme.css';
---

<div>
  <slot />
</div>

<style>
  div {
    padding: var(--size-3);
    border-radius: var(--radius-3);
    outline: 1px solid var(--font-mono);
    box-shadow: var(--shadow-4);
  }
</style>
